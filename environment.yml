name: smart-saw
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      # Async
      - asyncio
      - aiofiles

      # Modbus
      - pymodbus[asyncio]>=3.5.0

      # Database
      - aiosqlite
      - asyncpg

      # MQTT
      - aiomqtt>=1.2.0

      # GUI
      - PySide6>=6.4.0

      # ML
      - numpy>=1.24.0,<2.0
      - scikit-learn>=1.3.0
      - joblib>=1.3.0
      - pandas>=2.0.0

      # Config
      - pyyaml>=6.0
      - python-dotenv>=1.0.0

      # Logging
      - colorlog>=6.7.0
