---
phase: 21-ai-detection-pipeline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/camera/detection_worker.py
  - src/services/camera/modelB4.py
  - tests/test_detection_worker.py
autonomous: true
requirements: [DET-01, DET-02, DET-05, DET-06]

must_haves:
  truths:
    - "DetectionWorker publishes broken_count, tooth_count, broken_confidence to CameraResultsStore after broken model inference"
    - "DetectionWorker publishes crack_count, crack_confidence to CameraResultsStore after crack model inference"
    - "Importing detection_worker at module level does not trigger torch or ultralytics imports"
    - "modelB4.py has from __future__ import annotations and no __main__ test block"
  artifacts:
    - path: "tests/test_detection_worker.py"
      provides: "Mock-based unit tests for DetectionWorker"
      min_lines: 80
    - path: "src/services/camera/detection_worker.py"
      provides: "Convention-audited RT-DETR worker"
      contains: "class DetectionWorker"
    - path: "src/services/camera/modelB4.py"
      provides: "LDC neural network architecture (minimal cleanup)"
      contains: "class LDC"
  key_links:
    - from: "src/services/camera/detection_worker.py"
      to: "src/services/camera/results_store.py"
      via: "results_store.update_batch()"
      pattern: "update_batch"
    - from: "tests/test_detection_worker.py"
      to: "src/services/camera/detection_worker.py"
      via: "import and mock-based test"
      pattern: "from src.services.camera.detection_worker import DetectionWorker"
---

<objective>
Convention-audit DetectionWorker and modelB4, then write mock-based unit tests proving DET-01/DET-02/DET-05/DET-06.

Purpose: The existing detection_worker.py and modelB4.py have correct algorithmic logic from the old VisionService. This plan audits them for project conventions (docstrings, logging, type hints per CONVENTIONS.md) and writes comprehensive unit tests using the mock pattern established in test_camera_service.py. Per D-01, algorithmic logic (RT-DETR class mapping, inference loop, annotated frame saving) is preserved exactly.

Output: Convention-compliant detection_worker.py, cleaned modelB4.py, and test_detection_worker.py with passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-ai-detection-pipeline/21-CONTEXT.md
@.planning/phases/21-ai-detection-pipeline/21-RESEARCH.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/services/camera/results_store.py:
```python
class CameraResultsStore:
    def update(self, key: str, value: Any) -> None: ...
    def update_batch(self, data: dict[str, Any]) -> None: ...
    def get(self, key: str, default: Any = None) -> Any: ...
    def snapshot(self) -> dict[str, Any]: ...
```

From src/services/camera/camera_service.py:
```python
class CameraService:
    def get_current_frame(self) -> np.ndarray | None: ...
```

DetectionWorker published keys (existing, must be preserved):
- broken_count (int)
- broken_confidence (float)
- tooth_count (int)
- crack_count (int)
- crack_confidence (float)
- last_detection_ts (ISO string)

RT-DETR class mapping:
- Broken model: class 0 = tooth (healthy), class 1 = broken
- Crack model: class 0 = crack
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convention-audit detection_worker.py and modelB4.py</name>
  <files>src/services/camera/detection_worker.py, src/services/camera/modelB4.py</files>
  <read_first>
    - src/services/camera/detection_worker.py (current state — 311 lines)
    - src/services/camera/modelB4.py (current state — ~250 lines)
    - .planning/codebase/CONVENTIONS.md (target conventions)
    - tests/test_camera_service.py (reference mock pattern)
    - src/services/camera/results_store.py (API contract)
  </read_first>
  <action>
    **detection_worker.py — Convention audit (per D-01: algorithmic logic preserved exactly):**

    1. Verify module docstring is present and accurate (it already is — keep as-is or minor wording polish only)
    2. Verify `from __future__ import annotations` is present (already is)
    3. Verify `logger = logging.getLogger(__name__)` pattern (already matches)
    4. Verify all logging uses %-format positional args (camera module convention, NOT f-strings — camera_service.py established this pattern)
    5. Verify Google-style docstrings on all public methods: `__init__`, `run`, `stop`, `model_load_failed` property. Add `Args:` and `Returns:` sections if missing. Keep existing docstrings that are already correct.
    6. Verify type hints on all public method signatures (already present). Add missing type hints on internal methods if any:
       - `_save_annotated_frame(self, frame: np.ndarray, broken_results: list, crack_results: list) -> None` — add TYPE_CHECKING import for np.ndarray if needed, or use `Any` for result types since ultralytics Results are runtime-only
    7. DO NOT MODIFY: The `run()` method's inference loop, class mapping logic (class 0=tooth, class 1=broken; class 0=crack), `_save_annotated_frame()` bounding box colors (red=broken, green=tooth, blue=crack), DB write code blocks, or `update_batch()` key names.
    8. DO NOT MOVE: `import torch` and `from ultralytics import RTDETR` must stay inside `run()`. `import cv2` must stay inside `_save_annotated_frame()`.

    **modelB4.py — Minimal cleanup (per D-02):**

    1. Verify `from __future__ import annotations` is present (already is — line 1)
    2. Remove the `if __name__ == '__main__':` block at the bottom (lines 234-253) — this is development scaffolding, not needed in production
    3. DO NOT modify any NN architecture code (CoFusion, _DenseLayer, _DenseBlock, UpConvBlock, SingleConvBlock, DoubleConvBlock, LDC classes). These are the trained model's architecture and must match the checkpoint exactly.
    4. DO NOT change `import torch` / `import torch.nn` at module level — modelB4 is always imported inside `run()` so the lazy import contract is maintained transitively.
    5. Optionally add a brief module docstring if missing: `"""Lightweight Dense CNN (LDC) for edge detection — model architecture for 16_model.pth checkpoint."""`
  </action>
  <verify>
    <automated>cd /media/workspace/smart-saw && python3 -c "import ast; ast.parse(open('src/services/camera/detection_worker.py').read()); ast.parse(open('src/services/camera/modelB4.py').read()); print('SYNTAX OK')" && python3 -c "import src.services.camera.detection_worker; print('IMPORT OK — no torch/ultralytics triggered')" && python3 -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - detection_worker.py contains `from __future__ import annotations` on line 1 area
    - detection_worker.py contains `logger = logging.getLogger(__name__)`
    - detection_worker.py `run()` method still contains `import torch` and `from ultralytics import RTDETR` inside method body
    - detection_worker.py `_save_annotated_frame` still contains `import cv2` inside method body
    - detection_worker.py still contains `class_id == 1` check for broken tooth and `class_id == 0` for crack
    - detection_worker.py still contains `update_batch` call with keys: broken_count, broken_confidence, tooth_count, crack_count, crack_confidence, last_detection_ts
    - modelB4.py does NOT contain `if __name__` block
    - modelB4.py still contains `class LDC(nn.Module):`
    - `python3 -c "import src.services.camera.detection_worker"` succeeds without importing torch
    - All existing 17 tests still pass
  </acceptance_criteria>
  <done>detection_worker.py and modelB4.py are convention-compliant, algorithmic logic is preserved, and all existing tests still pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write unit tests for DetectionWorker</name>
  <files>tests/test_detection_worker.py</files>
  <read_first>
    - src/services/camera/detection_worker.py (implementation to test)
    - tests/test_camera_service.py (reference mock pattern — MagicMock + patch)
    - src/services/camera/results_store.py (CameraResultsStore API)
  </read_first>
  <behavior>
    - test_import_stays_lightweight: importing detection_worker module does NOT trigger torch or ultralytics imports (DET-06). Verify by checking sys.modules before and after import — torch/ultralytics should not appear.
    - test_constructor_sets_config: DetectionWorker(config, store, camera_service) reads detection config values (interval, confidence, model paths) from config dict without loading models or doing I/O.
    - test_publishes_broken_results: With mocked torch and RTDETR, after one detection cycle, store contains broken_count (int), tooth_count (int), broken_confidence (float), last_detection_ts (str). Mock broken_model.predict() to return a result with 2 boxes: class_id=0 (tooth) and class_id=1 (broken) with confidence 0.85. Verify store.get("tooth_count") == 1, store.get("broken_count") == 1, store.get("broken_confidence") == 0.85. (DET-01, DET-05)
    - test_publishes_crack_results: Same setup but for crack model. Mock crack_model.predict() to return 1 box with class_id=0 (crack) confidence 0.70. Verify store.get("crack_count") == 1, store.get("crack_confidence") == 0.70. (DET-02, DET-05)
    - test_db_write_guarded_by_db_service: With db_service=None (default per D-04), no DB write is attempted even when broken_count > 0. This is the Phase 21 default.
    - test_stop_event_exits_loop: Calling stop() causes the main loop to exit. Verify thread.is_alive() becomes False within 5 seconds.
  </behavior>
  <action>
    Create `tests/test_detection_worker.py` following the mock pattern from test_camera_service.py.

    **Test infrastructure:**
    ```python
    import sys
    import threading
    import time
    from unittest.mock import MagicMock, patch, PropertyMock
    import numpy as np
    import pytest
    from src.services.camera.results_store import CameraResultsStore
    ```

    **Config fixture:**
    ```python
    DET_CONFIG = {
        "detection": {
            "enabled": True,
            "interval_seconds": 0.1,  # fast for tests
            "confidence_threshold": 0.5,
            "broken_model_path": "data/models/best.pt",
            "crack_model_path": "data/models/catlak-best.pt",
        }
    }
    ```

    **Mock strategy for test_publishes_broken_results and test_publishes_crack_results:**
    - Patch `src.services.camera.detection_worker.torch` and `src.services.camera.detection_worker.RTDETR` at the module scope where they are imported inside `run()` — however, since imports are inside `run()`, the patch targets must be set up so that when `run()` does `import torch` and `from ultralytics import RTDETR`, the mocks are injected. The correct approach is:
      - `@patch("src.services.camera.detection_worker.torch", create=True)` won't work because the import is local.
      - Instead, patch the actual module: `@patch.dict("sys.modules", {"torch": mock_torch_module, "ultralytics": mock_ultralytics_module})` so that `import torch` and `from ultralytics import RTDETR` inside `run()` resolve to mocks.
    - Create mock RTDETR instances that return mock prediction results.
    - Create a mock result object with a `.boxes` attribute containing mock box objects. Each box has `.cls` (tensor-like with `.item()`), `.conf` (tensor-like with `.item()`), and `.xyxy` (for annotated frames).
    - Use `camera_service.get_current_frame.return_value = np.zeros((720, 1280, 3), dtype=np.uint8)` to provide a frame.
    - Set stop_event after first iteration by having the mock predict() side_effect set stop on second call.

    **Mock box helper:**
    ```python
    def _make_mock_box(class_id: int, confidence: float):
        box = MagicMock()
        cls_tensor = MagicMock()
        cls_tensor.__getitem__ = lambda self, idx: MagicMock(item=lambda: class_id)
        box.cls = cls_tensor
        conf_tensor = MagicMock()
        conf_tensor.__getitem__ = lambda self, idx: MagicMock(item=lambda: confidence)
        box.conf = conf_tensor
        xyxy_tensor = MagicMock()
        xyxy_tensor.__getitem__ = lambda self, idx: [10, 20, 100, 200]
        box.xyxy = xyxy_tensor
        return box
    ```

    **Key test logic for test_publishes_broken_results:**
    1. Create CameraResultsStore and mock CameraService
    2. Create mock torch module with `cuda.is_available()` returning False, `no_grad()` context manager
    3. Create mock ultralytics module with RTDETR class returning mock model
    4. Mock model.predict() for broken model: return [result_with_boxes([tooth_box, broken_box])]
    5. Mock model.predict() for crack model: return [result_with_boxes([])]  (no cracks)
    6. Inject mocks via sys.modules patching
    7. Start DetectionWorker, wait briefly, then stop
    8. Assert store.get("tooth_count") == 1, store.get("broken_count") == 1

    **For test_import_stays_lightweight (DET-06):**
    Record `set(sys.modules.keys())` before importing, import `src.services.camera.detection_worker`, check that "torch" and "ultralytics" are NOT in `sys.modules` (or were already there before).

    Write all 6 tests. Run `python3 -m pytest tests/test_detection_worker.py -x -v` to verify.
  </action>
  <verify>
    <automated>cd /media/workspace/smart-saw && python3 -m pytest tests/test_detection_worker.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_detection_worker.py exists and contains at least 6 test functions
    - tests/test_detection_worker.py contains `test_import_stays_lightweight`
    - tests/test_detection_worker.py contains `test_publishes_broken_results`
    - tests/test_detection_worker.py contains `test_publishes_crack_results`
    - tests/test_detection_worker.py contains `test_db_write_guarded_by_db_service`
    - tests/test_detection_worker.py contains `test_stop_event_exits_loop`
    - `python3 -m pytest tests/test_detection_worker.py -x -v` exits with code 0
    - `python3 -m pytest tests/ -x -q` exits with code 0 (all tests including existing 17 pass)
  </acceptance_criteria>
  <done>DetectionWorker has 6+ unit tests covering DET-01, DET-02, DET-05, DET-06; all tests pass; existing tests unaffected</done>
</task>

</tasks>

<verification>
- `python3 -m pytest tests/ -x -q` — all tests pass (existing 17 + new detection worker tests)
- `python3 -c "import src.services.camera.detection_worker; print('OK')"` — imports without triggering torch/ultralytics
- `python3 -c "import ast; ast.parse(open('src/services/camera/modelB4.py').read()); print('OK')"` — modelB4 syntax valid
- `grep -c 'if __name__' src/services/camera/modelB4.py` — returns 0 (test block removed)
</verification>

<success_criteria>
- DetectionWorker convention audit complete with algorithmic logic preserved (D-01)
- modelB4.py cleaned with minimal changes (D-02): __main__ block removed, module docstring present
- 6+ unit tests for DetectionWorker covering DET-01, DET-02, DET-05, DET-06
- All tests pass (existing + new)
</success_criteria>

<output>
After completion, create `.planning/phases/21-ai-detection-pipeline/21-01-SUMMARY.md`
</output>
