# Phase 19.3: Band Tension Graph - Context

**Gathered:** 2026-03-26
**Status:** Ready for planning

<domain>
## Phase Boundary

Extend the existing Y-axis button selection system in `sensor_controller.py` to include band tension (`serit_gerginligi_bar`) as a selectable Y-axis variable. No new graph widget, no new data pipeline, no new architecture — only modify `sensor_controller.py`.

</domain>

<decisions>
## Implementation Decisions

### New button
- Add a new Y-axis button labeled **"Şerit Gerginliği"** at position `x=298, y=238` inside `XEkseniFrame`
- Same `axis_btn_style`, same `setCheckable(True)`, same `QButtonGroup` membership as existing Y-axis buttons

### Existing button relocation
- Move `btnSeritTork` from its current position `(169, 239)` to `x=41, y=238`

### Data key mapping
- New button maps to `serit_gerginligi_bar` in `_get_selected_y_axis()`
- Add `"serit_gerginligi_bar": "Şerit Gerginliği (bar)"` to the `y_titles` dict in `update_axis_titles()`

### Button group registration
- Add `'btnSeritGerginligi'` to the `_setup_axis_button_groups()` loop over Y-axis button names

### Behavior
- Clicking the new button sets the Y-axis variable to `serit_gerginligi_bar`
- Triggers the same `_on_y_axis_changed` flow as all other Y-axis buttons
- No changes to graph rendering, data pipeline, or anomaly logic

### Claude's Discretion
- Exact button widget attribute name (suggest: `self.btnSeritGerginligi`)

</decisions>

<specifics>
## Specific Ideas

- User explicitly stated: "Analyze existing Y-axis button implementations and replicate the exact pattern. Do not introduce new architecture, classes, or graph logic."
- Only `sensor_controller.py` should be modified.

</specifics>

<code_context>
## Existing Code Insights

### Reusable Assets
- `axis_btn_style` (line ~642 in sensor_controller.py): shared QPushButton stylesheet — use as-is
- `_y_group` (QButtonGroup): already wires up exclusive selection and `_on_y_axis_changed` signal
- `_setup_axis_button_groups()` (line 976): iterates button names by string — just add `'btnSeritGerginligi'` to the list

### Established Patterns
- Y-axis button pattern: create `QPushButton` → set geometry → set style → `setCheckable(True)` → (group registration handled in `_setup_axis_button_groups`)
- `_get_selected_y_axis()` uses `hasattr` + `isChecked()` chain — add a new `elif` branch
- `update_axis_titles()` uses a `y_titles` dict — add one key

### Integration Points
- `XEkseniFrame` (line 681): parent widget for all Y-axis buttons — new button is a child of this frame
- `_setup_axis_button_groups()` (line 981): add `'btnSeritGerginligi'` to the name list
- `_get_selected_y_axis()` (line 1019): add `elif hasattr(self, 'btnSeritGerginligi') and self.btnSeritGerginligi.isChecked(): return "serit_gerginligi_bar"`
- `update_axis_titles()` (line 102): add `"serit_gerginligi_bar": "Şerit Gerginliği (bar)"` to `y_titles`

</code_context>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope.

</deferred>

---

*Phase: 19.3-band-tension-graph*
*Context gathered: 2026-03-26*
