---
phase: 24.1-config-fixes-requirements-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/config.yaml
  - src/services/camera/health_calculator.py
  - src/services/camera/ldc_worker.py
  - tests/test_health_calculator.py
  - tests/test_ldc_worker.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "config.yaml camera bolumunde vision alt bolumu mevcut ve polling_interval + recording_duration tanimli"
    - "HealthCalculator constructor'dan broken_weight ve wear_weight kabul ediyor; default degerler 0.7 ve 0.3"
    - "LDCWorker __init__'de health config degerlerini cekip HealthCalculator'e aktariyor"
    - "Mevcut testler kirilmadan geciyor; yeni testler config-driven weight davranisini dogruluyor"
  artifacts:
    - path: "config/config.yaml"
      provides: "camera.vision subsection"
      contains: "vision:"
    - path: "src/services/camera/health_calculator.py"
      provides: "Config-driven HealthCalculator"
      contains: "def __init__"
    - path: "src/services/camera/ldc_worker.py"
      provides: "Health weight extraction in __init__"
      contains: "self._broken_weight"
    - path: "tests/test_health_calculator.py"
      provides: "Custom weight constructor test"
      contains: "test_custom_weights"
    - path: "tests/test_ldc_worker.py"
      provides: "Health config extraction test"
      contains: "test_constructor_reads_health_weights_from_config"
  key_links:
    - from: "config/config.yaml"
      to: "src/services/camera/vision_service.py"
      via: "config.get('vision', {})"
      pattern: "vision_cfg.get.*polling_interval"
    - from: "config/config.yaml"
      to: "src/services/camera/ldc_worker.py"
      via: "config.get('health', {}) in __init__"
      pattern: "health_cfg.get.*broken_weight"
    - from: "src/services/camera/ldc_worker.py"
      to: "src/services/camera/health_calculator.py"
      via: "HealthCalculator(broken_weight=self._broken_weight, wear_weight=self._wear_weight)"
      pattern: "HealthCalculator.*broken_weight.*wear_weight"
---

<objective>
v2.0 milestone auditinde tespit edilen iki config-kod uyumsuzlugunu kapatmak:
(P1) config.yaml'a eksik camera.vision alt bolumunu eklemek,
(P2) HealthCalculator'i config-driven hale getirip LDCWorker'dan agirlik degerlerini aktarmak.

Purpose: Config dosyasindaki tum kamera ayarlarinin gercekten kod tarafindan okunmasini saglamak —
artik "olu config anahtari" veya "eksik config bolumu" kalmayacak.

Output: Guncellemis config.yaml, refactor edilmis HealthCalculator ve LDCWorker, genisletilmis testler.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24.1-config-fixes-requirements-cleanup/24.1-RESEARCH.md

<interfaces>
<!-- Executor'un bilmesi gereken mevcut arayuzler -->

From src/services/camera/health_calculator.py (MEVCUT — degistirilecek):
```python
class HealthCalculator:
    BROKEN_WEIGHT = 0.7   # sinif sabiti — instance degiskenine donusturulecek
    WEAR_WEIGHT = 0.3     # sinif sabiti — instance degiskenine donusturulecek

    def calculate_broken_percentage(self, detected_teeth: int, detected_broken: int) -> float: ...
    def calculate_saw_health(self, detected_teeth: int, detected_broken: int, wear_percentage: float) -> float: ...
    def get_health_status(self, saw_health: float) -> str: ...
    def get_health_color(self, saw_health: float) -> str: ...
```

From src/services/camera/ldc_worker.py __init__ (MEVCUT — genisletilecek):
```python
class LDCWorker(threading.Thread):
    def __init__(self, config: dict, results_store, camera_service, db_service=None) -> None:
        # config.get("wear", {}) ile wear parametreleri cikartiliyor
        # config.get("health", {}) kullanilmiyor — EKLENECEK
```

From src/services/camera/ldc_worker.py run() satir 80 (MEVCUT — degistirilecek):
```python
health_calc = HealthCalculator()  # config gecirilmiyor — DEGISTIRILECEK
```

From config/config.yaml satir 559-562 (MEVCUT):
```yaml
  health:
    broken_weight: 0.70
    wear_weight: 0.30
```

From config/config.yaml (EKSIK — EKLENECEK):
```yaml
  # vision: alt bolumu yok — eklenecek
```

From src/services/camera/vision_service.py satir 57-63 (REFERANS — degistirilmeyecek):
```python
vision_cfg = config.get("vision", {})
self._polling_interval: float = float(vision_cfg.get("polling_interval", 0.5))
self._recording_duration: float = float(vision_cfg.get("recording_duration", 10.0))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: config.yaml'a vision alt bolumu ekle + HealthCalculator'i config-driven yap</name>
  <files>config/config.yaml, src/services/camera/health_calculator.py, src/services/camera/ldc_worker.py</files>
  <read_first>
    config/config.yaml
    src/services/camera/health_calculator.py
    src/services/camera/ldc_worker.py
    src/services/camera/vision_service.py
  </read_first>
  <action>
Uc dosyada degisiklik yap:

**1. config/config.yaml — vision alt bolumu ekle:**

`health:` bolumunun ALTINA (satir 562'den sonra), `camera:` kapsaminda, ayni indent seviyesinde (2 bosluk) su blogu ekle:

```yaml
  # Vision service orchestration
  vision:
    polling_interval: 0.5      # Saniyede kac kez testere_durumu kontrol edilir
    recording_duration: 10.0   # Kesim bitiminde kac saniye kayit yapilir
```

DIKKAT: indent seviyesi `health:` ile ayni olmali (2 bosluk). Bu blok `camera:` altinda olmali. YAML parse hatasi olmasin diye mevcut yapiyi bozmamaya dikkat et.

**2. src/services/camera/health_calculator.py — constructor ekle:**

Mevcut `BROKEN_WEIGHT = 0.7` ve `WEAR_WEIGHT = 0.3` sinif sabitlerini KALDIR. Yerine `__init__` metodu ekle:

```python
def __init__(self, broken_weight: float = 0.7, wear_weight: float = 0.3) -> None:
    """Initialise with configurable impact weights.

    Args:
        broken_weight: Weight for broken-tooth impact (default 0.7).
        wear_weight: Weight for wear impact (default 0.3).
    """
    self.BROKEN_WEIGHT = broken_weight
    self.WEAR_WEIGHT = wear_weight
```

Bu degisiklik geriye uyumlu — `HealthCalculator()` cagrildiginda default degerler 0.7/0.3 olarak kalir. Mevcut `calculate_saw_health` icindeki `self.BROKEN_WEIGHT` / `self.WEAR_WEIGHT` referanslari aynen calisir cunku artik instance degiskeni.

**3. src/services/camera/ldc_worker.py — health weight extraction ekle:**

`__init__` metodunda, mevcut `self._model_load_failed: bool = False` satirindan ONCE, su satirlari ekle:

```python
        # Health calculator weights (from camera.health config)
        health_cfg = config.get("health", {})
        self._broken_weight: float = float(health_cfg.get("broken_weight", 0.7))
        self._wear_weight: float = float(health_cfg.get("wear_weight", 0.3))
```

`run()` metodunda satir 80'deki `health_calc = HealthCalculator()` satirini su sekilde degistir:

```python
        health_calc = HealthCalculator(
            broken_weight=self._broken_weight,
            wear_weight=self._wear_weight,
        )
```
  </action>
  <verify>
    <automated>cd /media/workspace/smart-saw && python -c "import yaml; c=yaml.safe_load(open('config/config.yaml')); assert 'vision' in c['camera'], 'vision missing'; assert c['camera']['vision']['polling_interval'] == 0.5; assert c['camera']['vision']['recording_duration'] == 10.0; print('P1 OK')" && python -c "from src.services.camera.health_calculator import HealthCalculator; h=HealthCalculator(broken_weight=0.5, wear_weight=0.5); assert h.BROKEN_WEIGHT == 0.5; assert h.WEAR_WEIGHT == 0.5; h2=HealthCalculator(); assert h2.BROKEN_WEIGHT == 0.7; print('P2 OK')"</automated>
  </verify>
  <acceptance_criteria>
    - config/config.yaml icerisinde `vision:` alt bolumu `camera:` altinda mevcut
    - config/config.yaml icerisinde `polling_interval: 0.5` satiri mevcut
    - config/config.yaml icerisinde `recording_duration: 10.0` satiri mevcut
    - health_calculator.py icerisinde `def __init__(self, broken_weight: float = 0.7, wear_weight: float = 0.3)` mevcut
    - health_calculator.py icerisinde `BROKEN_WEIGHT = 0.7` sinif sabiti KALDIRILMIS (grep donus vermez)
    - ldc_worker.py icerisinde `self._broken_weight` ve `self._wear_weight` mevcut
    - ldc_worker.py icerisinde `HealthCalculator(` satiri `broken_weight=` parametresi iceriyor
    - `python -c "import yaml; c=yaml.safe_load(open('config/config.yaml')); assert 'vision' in c['camera']"` basariyla calisir
  </acceptance_criteria>
  <done>
    config.yaml'da camera.vision bolumu mevcut ve parse ediliyor; HealthCalculator config-driven constructor'a sahip; LDCWorker health weight degerlerini config'den okuyup HealthCalculator'e aktariyor.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mevcut testleri guncelle + yeni config-driven testler ekle</name>
  <files>tests/test_health_calculator.py, tests/test_ldc_worker.py</files>
  <read_first>
    tests/test_health_calculator.py
    tests/test_ldc_worker.py
    src/services/camera/health_calculator.py
    src/services/camera/ldc_worker.py
  </read_first>
  <behavior>
    - Test: HealthCalculator() default weights 0.7/0.3 ile calisir (mevcut testler kirilmamali)
    - Test: HealthCalculator(broken_weight=0.5, wear_weight=0.5) custom degerler ile dogru hesaplar
    - Test: HealthCalculator(broken_weight=1.0, wear_weight=0.0) edge case — yalnizca kirik etkisi
    - Test: LDCWorker constructor health config'den _broken_weight ve _wear_weight cikartir
    - Test: LDCWorker default config (health bolumu yok) iken default 0.7/0.3 kullanir
  </behavior>
  <action>
**1. tests/test_health_calculator.py — yeni testler ekle:**

Dosyanin sonuna su testleri ekle:

```python
# ---------------------------------------------------------------------------
# Custom weight tests
# ---------------------------------------------------------------------------


def test_custom_weights_affect_health() -> None:
    """HealthCalculator with custom weights produces different health score.

    With broken_weight=0.5, wear_weight=0.5:
    10 teeth, 3 broken (30%), 50% wear:
    health = 100 - ((0.30 * 0.5 + 0.50 * 0.5) * 100)
           = 100 - ((0.15 + 0.25) * 100)
           = 100 - 40
           = 60.0
    """
    calc = HealthCalculator(broken_weight=0.5, wear_weight=0.5)
    result = calc.calculate_saw_health(10, 3, 50.0)
    assert result == pytest.approx(60.0)


def test_custom_weights_only_broken() -> None:
    """HealthCalculator with broken_weight=1.0, wear_weight=0.0 ignores wear.

    10 teeth, 3 broken (30%), 80% wear:
    health = 100 - ((0.30 * 1.0 + 0.80 * 0.0) * 100)
           = 100 - 30
           = 70.0
    """
    calc = HealthCalculator(broken_weight=1.0, wear_weight=0.0)
    result = calc.calculate_saw_health(10, 3, 80.0)
    assert result == pytest.approx(70.0)


def test_default_constructor_unchanged() -> None:
    """HealthCalculator() without args still uses 0.7/0.3 defaults."""
    calc = HealthCalculator()
    assert calc.BROKEN_WEIGHT == pytest.approx(0.7)
    assert calc.WEAR_WEIGHT == pytest.approx(0.3)
```

**2. tests/test_ldc_worker.py — health config test ekle:**

LDC_CONFIG fixture'una `health` bolumu ekle (mevcut `wear` yanina):

```python
LDC_CONFIG = {
    "wear": {
        "enabled": True,
        "interval_seconds": 0.1,
        "ldc_checkpoint_path": "data/models/ldc/16_model.pth",
        "top_line_y": 170,
        "bottom_line_y": 236,
        "roi_x_center_n": 0.500000,
        "roi_y_center_n": 0.530692,
        "roi_width_n": 0.736888,
        "roi_height_n": 0.489510,
        "bgr_mean": [103.939, 116.779, 123.68],
    },
    "health": {
        "broken_weight": 0.70,
        "wear_weight": 0.30,
    },
}
```

CUSTOM_ROI_CONFIG'e de ayni `health` bolumunu ekle.

Dosyanin sonuna su testleri ekle:

```python
# ---------------------------------------------------------------------------
# Test 9: Constructor reads health weights from config
# ---------------------------------------------------------------------------


def test_constructor_reads_health_weights_from_config() -> None:
    """LDCWorker reads broken_weight and wear_weight from config.health section."""
    from src.services.camera.ldc_worker import LDCWorker

    store = CameraResultsStore()
    mock_camera = MagicMock()

    custom_config = {
        "wear": LDC_CONFIG["wear"],
        "health": {
            "broken_weight": 0.50,
            "wear_weight": 0.50,
        },
    }

    worker = LDCWorker(custom_config, store, mock_camera)

    assert worker._broken_weight == pytest.approx(0.50)
    assert worker._wear_weight == pytest.approx(0.50)


# ---------------------------------------------------------------------------
# Test 10: Default health weights when no health config section
# ---------------------------------------------------------------------------


def test_default_health_weights_without_config() -> None:
    """LDCWorker uses default 0.7/0.3 when config has no health section."""
    from src.services.camera.ldc_worker import LDCWorker

    store = CameraResultsStore()
    mock_camera = MagicMock()

    minimal_config = {
        "wear": LDC_CONFIG["wear"],
        # no "health" key
    }

    worker = LDCWorker(minimal_config, store, mock_camera)

    assert worker._broken_weight == pytest.approx(0.7)
    assert worker._wear_weight == pytest.approx(0.3)
```

ONEMLI: Mevcut test numaralama sistemi korunmali — yeni LDC testleri Test 9 ve Test 10 olarak devam eder (mevcut Test 8'den sonra). Mevcut `calc` fixture'ini kullanan testlerin degismeden gectiginden emin ol.
  </action>
  <verify>
    <automated>cd /media/workspace/smart-saw && python -m pytest tests/test_health_calculator.py tests/test_ldc_worker.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_health_calculator.py icerisinde `test_custom_weights_affect_health` fonksiyonu mevcut
    - tests/test_health_calculator.py icerisinde `test_custom_weights_only_broken` fonksiyonu mevcut
    - tests/test_health_calculator.py icerisinde `test_default_constructor_unchanged` fonksiyonu mevcut
    - tests/test_ldc_worker.py icerisinde `test_constructor_reads_health_weights_from_config` fonksiyonu mevcut
    - tests/test_ldc_worker.py icerisinde `test_default_health_weights_without_config` fonksiyonu mevcut
    - tests/test_ldc_worker.py LDC_CONFIG dict'inde `"health"` anahtari mevcut
    - `python -m pytest tests/test_health_calculator.py -x -q` cikis kodu 0
    - `python -m pytest tests/test_ldc_worker.py -x -q` cikis kodu 0
    - Tum mevcut testler (8 health_calculator + 8 ldc_worker) kirilmadan geciyor
  </acceptance_criteria>
  <done>
    Tum mevcut testler gecmeye devam ediyor; yeni testler config-driven HealthCalculator davranisini ve LDCWorker health config extraction'ini dogruluyor. pytest cikis kodu 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.yaml -> Python | Config dosyasindan okunan degerler float() ile cast ediliyor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24.1-01 | Tampering | config.yaml | accept | Config dosyasi sunucuda yerel; yalnizca admin erisimi var. Gecersiz float degeri ValueError firlatir — HealthCalculator default'a duser. Dusuk riskli ortam (endustriyel panel PC, fiziksel erisim sinirli). |
| T-24.1-02 | Denial of Service | HealthCalculator | accept | broken_weight/wear_weight negatif veya >1.0 verilirse saglik skoru yanlis hesaplanir ama uygulama crash etmez — float aritmetik her degerde calisir. Config validasyonu mevcut ASVS L1 kapsaminda gerekli degil (local-only config). |
</threat_model>

<verification>
1. `python -c "import yaml; c=yaml.safe_load(open('config/config.yaml')); assert 'vision' in c['camera']; print('P1: vision config OK')"` — 0 cikis kodu
2. `python -c "from src.services.camera.health_calculator import HealthCalculator; h=HealthCalculator(0.5,0.5); assert h.BROKEN_WEIGHT==0.5; print('P2: config-driven OK')"` — 0 cikis kodu
3. `python -m pytest tests/test_health_calculator.py tests/test_ldc_worker.py -x -q` — tumu pass, 0 cikis kodu
4. `grep -c 'BROKEN_WEIGHT = 0.7' src/services/camera/health_calculator.py` — 0 donus (sinif sabiti kaldirilmis)
5. `grep 'self._broken_weight' src/services/camera/ldc_worker.py` — en az 2 satir (atama + kullanim)
</verification>

<success_criteria>
- config.yaml'da camera.vision bolumu parse edilebiliyor (polling_interval=0.5, recording_duration=10.0)
- HealthCalculator constructor'dan broken_weight/wear_weight kabul ediyor, default degerler 0.7/0.3
- LDCWorker config.health'ten agirlik degerlerini cekip HealthCalculator'e aktariyor
- Tum mevcut testler kirilmadan geciyor
- Yeni testler config-driven davranisi dogruluyor
- REQUIREMENTS.md'de GUI-06 zaten [x] — ek degisiklik gerekmiyor
</success_criteria>

<output>
After completion, create `.planning/phases/24.1-config-fixes-requirements-cleanup/24.1-01-SUMMARY.md`
</output>
