---
phase: 19.3-band-tension-graph
plan: 01
subsystem: ui
tags: [pyqt5, sensor-graph, y-axis-buttons, band-tension]

# Dependency graph
requires:
  - phase: 19.2-graph-axis-labels
    provides: axis title label infrastructure in sensor_controller.py
provides:
  - btnSeritGerginligi QPushButton in XEkseniFrame with geometry (298, 238, 240, 69)
  - serit_gerginligi_bar selectable as 6th Y-axis variable in cutting graph
  - Y-axis title "Şerit Gerginliği (bar)" displayed when new button is active
affects: [sensor_controller, cutting-graph-ui]

# Tech tracking
tech-stack:
  added: []
  patterns: [existing Y-axis button pattern extended with 6th entry — same 4-touch-point wiring as existing 5 buttons]

key-files:
  created: []
  modified:
    - src/gui/controllers/sensor_controller.py

key-decisions:
  - "btnSeritTork relocated from (169, 239) to (41, 238) so btnSeritGerginligi can occupy (298, 238) in the same bottom row — pair symmetry matches rows above"
  - "serit_gerginligi_bar data key was already fully wired through Modbus, domain model, DB schema, and IoT — only the UI selection button was missing"

patterns-established:
  - "6-button Y-axis group pattern: 2 rows of 2 + 1 row of 2; buttons at y=82, y=162, y=238; left x=41, right x=298"

requirements-completed: []

# Metrics
duration: 3min
completed: 2026-03-26
---

# Phase 19.3 Plan 01: Band Tension Graph Summary

**6th Y-axis button (Şerit Gerginliği) added to cutting graph panel by relocating Şerit Tork and threading serit_gerginligi_bar through 4 integration points in sensor_controller.py**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-03-26T08:43:59Z
- **Completed:** 2026-03-26T08:47:00Z
- **Tasks:** 1 of 2 complete (checkpoint:human-verify pending)
- **Files modified:** 1

## Accomplishments
- Added `btnSeritGerginligi` QPushButton at geometry `(298, 238, 240, 69)` inside XEkseniFrame with `axis_btn_style` and `setCheckable(True)`
- Relocated `btnSeritTork` from `(169, 239, 240, 69)` to `(41, 238, 240, 69)` so both buttons form a symmetric pair in the bottom row
- Registered `btnSeritGerginligi` in `_setup_axis_button_groups()` name tuple alongside the 5 existing buttons
- Added `elif hasattr(self, 'btnSeritGerginligi') and self.btnSeritGerginligi.isChecked(): return "serit_gerginligi_bar"` in `_get_selected_y_axis()` before the default else
- Added `"serit_gerginligi_bar": "Şerit Gerginliği (bar)"` to the `y_titles` dict in `update_axis_titles()`

## Task Commits

Each task was committed atomically:

1. **Task 1: Add btnSeritGerginligi and thread through 4 integration points** - `ab506ab` (feat)

**Plan metadata:** TBD (docs: complete plan — pending after checkpoint)

## Files Created/Modified
- `src/gui/controllers/sensor_controller.py` - 4 surgical edits: button creation, geometry relocation, group registration, elif branch, y_titles entry

## Decisions Made
- Relocated btnSeritTork to x=41 (from x=169 center) to mirror the row pattern of rows 1 and 2, placing btnSeritGerginligi symmetrically at x=298
- No other files required: QPushButton and axis_btn_style were already in scope; serit_gerginligi_bar was already wired through the data pipeline

## Deviations from Plan

None - plan executed exactly as written. All 4 edits made with no unexpected issues.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Task 1 complete and committed. Awaiting human verification (checkpoint:human-verify) to confirm the new button appears and functions correctly in the UI.
- After checkpoint approval, the plan will be fully complete.
- serit_gerginligi_bar is already present in the Modbus reader, domain model, DB schema, and processor — the UI button was the only missing piece.

---
*Phase: 19.3-band-tension-graph*
*Completed: 2026-03-26*
