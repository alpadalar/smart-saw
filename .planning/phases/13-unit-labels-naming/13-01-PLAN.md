---
phase: 13-unit-labels-naming
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/gui/controllers/control_panel_controller.py
  - src/gui/controllers/monitoring_controller.py
autonomous: true
---

<objective>
Add units (mm/dk, m/dk, A, %) to numerical labels and rename "İnme Hızı" → "İlerleme Hızı" across all GUI pages.

Purpose: Improve UX by showing units next to values and using consistent Turkish terminology (İlerleme = progression/advance, more appropriate than İnme = descent).
Output: Updated GUI labels with units and consistent naming.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Source files to modify:
@src/gui/controllers/control_panel_controller.py
@src/gui/controllers/monitoring_controller.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add units and rename labels in control_panel_controller.py</name>
  <files>src/gui/controllers/control_panel_controller.py</files>
  <action>
Update the following label texts:

1. Line ~919: "Şerit Kesme Hızı" → "Şerit Kesme Hızı (m/dk)"
2. Line ~973: "Şerit Motor Akım" → "Şerit Motor Akımı (A)"
3. Line ~1006: "Şerit Motor Tork" → "Şerit Motor Torku (%)"
4. Line ~1041: "Şerit İnme Hızı" → "Şerit İlerleme Hızı (mm/dk)"
5. Line ~1095: "İnme Motor Akım" → "İlerleme Motor Akımı (A)"
6. Line ~1128: "İnme Motor Tork" → "İlerleme Motor Torku (%)"

Note: Only change the visible label text strings in QLabel() calls. Do NOT change:
- Variable names (they can stay as `labelBandDescentSpeed`, `BandDescentCurrentFrame` etc.)
- Dictionary keys or data field names
- Comments can stay as they are
- Log messages are OK to keep using "inme" terminology (internal)

This ensures minimal code churn while improving user-facing text.
  </action>
  <verify>grep -n "Şerit Kesme Hızı\|Şerit İlerleme Hızı\|Motor Akımı\|Motor Torku" src/gui/controllers/control_panel_controller.py shows all 6 labels have units and correct naming</verify>
  <done>All 6 label strings in control_panel_controller.py updated with units and İlerleme terminology</done>
</task>

<task type="auto">
  <name>Task 2: Add units and rename labels in monitoring_controller.py</name>
  <files>src/gui/controllers/monitoring_controller.py</files>
  <action>
Update the following label texts:

1. Line ~279: "Şerit Motor Hızı" → "Şerit Motor Hızı (m/dk)"
2. Line ~293: "İnme Motor Hızı" → "İlerleme Motor Hızı (mm/dk)"

Note: The following labels ALREADY have units (no change needed):
- Line ~307: "Şerit Motor Akımı (A)" - already has unit
- Line ~321: "İnme Motor Akımı (A)" - already has unit, but rename to "İlerleme Motor Akımı (A)"
- Line ~335: "Şerit Motor Torku (%)" - already has unit
- Line ~349: "İnme Motor Torku (%)" - already has unit, but rename to "İlerleme Motor Torku (%)"
- Line ~363: "Güç (kWh)" - already has unit

So update these 4 labels total:
1. "Şerit Motor Hızı" → "Şerit Motor Hızı (m/dk)"
2. "İnme Motor Hızı" → "İlerleme Motor Hızı (mm/dk)"
3. "İnme Motor Akımı (A)" → "İlerleme Motor Akımı (A)"
4. "İnme Motor Torku (%)" → "İlerleme Motor Torku (%)"

Again, only change visible label text strings. Keep variable names, comments, data keys as-is.
  </action>
  <verify>grep -n "Motor Hızı\|Motor Akımı\|Motor Torku" src/gui/controllers/monitoring_controller.py shows units on all labels and İlerleme instead of İnme</verify>
  <done>All 4 labels in monitoring_controller.py updated with units and İlerleme terminology</done>
</task>

<task type="auto">
  <name>Task 3: Verify no broken references and application runs</name>
  <files>src/gui/controllers/control_panel_controller.py, src/gui/controllers/monitoring_controller.py</files>
  <action>
Run a quick syntax check on both modified files:
1. python -m py_compile src/gui/controllers/control_panel_controller.py
2. python -m py_compile src/gui/controllers/monitoring_controller.py

Both should compile without syntax errors.

Also verify by grep that no "İnme Motor" labels remain (İnme should only appear in variable names, comments, data keys):
grep -n '"İnme Motor' src/gui/controllers/*.py should return no results (quoted string indicates label text)
  </action>
  <verify>py_compile succeeds for both files AND grep '"İnme Motor' returns no results</verify>
  <done>Both files compile without errors, no "İnme Motor" label strings remain</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All label changes applied (10 total: 6 in control_panel, 4 in monitoring)
- [ ] python -m py_compile passes for both files
- [ ] No "İnme Motor" quoted strings remain in GUI controllers
- [ ] Variable names left unchanged (minimal code churn)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No syntax errors introduced
- GUI labels now show units and use "İlerleme" consistently
</success_criteria>

<output>
After completion, create `.planning/phases/13-unit-labels-naming/13-01-SUMMARY.md`
</output>
