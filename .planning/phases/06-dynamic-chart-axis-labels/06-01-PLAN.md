---
phase: 06-dynamic-chart-axis-labels
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/gui/controllers/sensor_controller.py
autonomous: true
---

<objective>
Add dynamic axis title labels to the cutting graph that update based on selected X/Y axis buttons.

Purpose: Improve chart readability by showing what metric and unit each axis represents.
Output: Two new QLabel elements (Y-axis title on left, X-axis title on bottom) that update when axis selection changes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/gui/controllers/sensor_controller.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Y-axis title label to CuttingGraphWidget</name>
  <files>src/gui/controllers/sensor_controller.py</files>
  <action>
In CuttingGraphWidget._create_graph_labels(), add a new QLabel `self.y_axis_title` positioned vertically along the left side of the graph (rotated 90 degrees or use vertical text).

Position: Left of the Y-axis value labels (yust/yorta/yalt), centered vertically.
Suggested geometry: x=-30 from graph left edge, centered on graph height.

Style: Same font family "Plus Jakarta Sans", bold, 16px, color #F4F6FC.

Add method `update_axis_titles(x_axis: str, y_axis: str)` that sets title text based on axis type:
- y_axis == "serit_kesme_hizi" -> "Kesme Hizi (m/min)"
- y_axis == "serit_inme_hizi" -> "Ilerleme Hizi (mm/s)"
- y_axis == "serit_motor_akim_a" -> "Serit Akim (A)"
- y_axis == "serit_sapmasi" -> "Serit Sapmasi (mm)"
- y_axis == "serit_motor_tork_percentage" -> "Serit Tork (%)"

Call update_axis_titles() from set_axis_types() after updating the axis type attributes.

Note: Use Turkish characters in labels (i with dot = i, i without dot = ı). Example: "Hızı" not "Hizi".
  </action>
  <verify>Run application, select different Y-axis buttons, verify label text changes correctly.</verify>
  <done>Y-axis title label displays correct metric name and unit, updates on button click.</done>
</task>

<task type="auto">
  <name>Task 2: Add X-axis title label to CuttingGraphWidget</name>
  <files>src/gui/controllers/sensor_controller.py</files>
  <action>
In CuttingGraphWidget._create_graph_labels(), add a new QLabel `self.x_axis_title` positioned below the X-axis value labels.

Position: Below xust/xorta/xalt labels, centered horizontally on graph width.
Suggested geometry: y = graph_y + graph_height + 35 (below value labels), centered on graph width.

Style: Same as Y-axis title (Plus Jakarta Sans, bold, 16px, #F4F6FC).

In update_axis_titles(), add X-axis title mapping:
- x_axis == "timestamp" -> "Zaman (s)"
- x_axis == "kafa_yuksekligi_mm" -> "Yukseklik (mm)"

Note: Use proper Turkish characters: "Yükseklik" not "Yukseklik".
  </action>
  <verify>Run application, toggle between Zaman/Yukseklik buttons, verify X-axis title updates.</verify>
  <done>X-axis title label displays correct metric name and unit, updates on button click.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Y-axis title label visible and correctly positioned
- [ ] X-axis title label visible and correctly positioned
- [ ] Y-axis title updates when btnKesmeHizi/btnIlerlemeHizi/btnSeritAkim/btnSeritSapmasi/btnSeritTork clicked
- [ ] X-axis title updates when btnZaman/btnYukseklik clicked
- [ ] Labels use correct Turkish characters and units
- [ ] No visual overlap with existing value labels
- [ ] Application starts without errors
</verification>

<success_criteria>

- Both axis title labels created and positioned
- Titles update dynamically when axis buttons are clicked
- Turkish characters and units display correctly
- No regressions in existing chart functionality
</success_criteria>

<output>
After completion, create `.planning/phases/06-dynamic-chart-axis-labels/06-01-SUMMARY.md`
</output>
