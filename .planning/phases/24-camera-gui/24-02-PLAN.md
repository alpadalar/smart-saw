---
phase: 24-camera-gui
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - src/gui/controllers/main_controller.py
autonomous: false
requirements: [GUI-06]

must_haves:
  truths:
    - "Sidebar camera button appears only when camera.enabled=true (camera_results_store is not None)"
    - "Camera page at stack index 4 is only added when camera is enabled"
    - "closeEvent stops camera page timers via hasattr guard"
    - "Page index 4 is never accessed when camera is disabled"
  artifacts:
    - path: "src/gui/controllers/main_controller.py"
      provides: "Conditional camera sidebar button and page integration"
      contains: "camera_results_store is not None"
  key_links:
    - from: "src/gui/controllers/main_controller.py"
      to: "CameraController"
      via: "conditional import and page creation at line 297-303"
      pattern: "from .camera_controller import CameraController"
    - from: "src/gui/controllers/main_controller.py"
      to: "CameraController.stop_timers()"
      via: "closeEvent hasattr guard at line 416-417"
      pattern: "camera_page.*stop_timers"
---

<objective>
Audit MainController camera integration and visually verify the complete camera page.

Purpose: Confirm MainController's conditional camera guards are correct (button + page created together, timers stopped on close, no index-4 access when camera disabled). Then verify the full camera page visually.

Output: Audited main_controller.py (if any fixes needed) and visual confirmation of the complete camera page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-camera-gui/24-CONTEXT.md
@.planning/phases/24-camera-gui/24-UI-SPEC.md
@.planning/phases/24-camera-gui/24-01-SUMMARY.md

@src/gui/controllers/main_controller.py
@src/gui/controllers/camera_controller.py

<interfaces>
<!-- MainController camera integration points (from code read) -->

From src/gui/controllers/main_controller.py:
```python
# Line 212-220: Conditional camera button
if self.camera_results_store is not None:
    self.btnCamera = QPushButton("  Kamera", self.sidebarFrame)
    self.btnCamera.setGeometry(26, 649, 355, 110)
    # ... icon, style, connect to _switch_page(4) ...
    self.nav_buttons.append(self.btnCamera)

# Line 297-303: Conditional camera page
if self.camera_results_store is not None:
    from .camera_controller import CameraController
    self.camera_page = CameraController(self.camera_results_store, parent=self.stackedWidget)
    self.stackedWidget.addWidget(self.camera_page)  # Index 4

# Line 416-417: closeEvent camera timer stop
if hasattr(self, 'camera_page') and self.camera_page and hasattr(self.camera_page, 'stop_timers'):
    self.camera_page.stop_timers()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit MainController camera integration guards</name>
  <files>src/gui/controllers/main_controller.py</files>
  <read_first>
    - src/gui/controllers/main_controller.py (full file — focus on lines 212-220, 297-303, 416-417)
    - .planning/phases/24-camera-gui/24-UI-SPEC.md (section 6.8 — sidebar camera button spec)
    - .planning/phases/24-camera-gui/24-CONTEXT.md (decision D-08 — MainController audit scope)
  </read_first>
  <action>
Per D-08, audit MainController camera integration. Verify these three guards and fix if needed:

**Guard 1 — Sidebar button (line 212-220):**
Verify that `if self.camera_results_store is not None:` guard wraps:
- `self.btnCamera = QPushButton("  Kamera", self.sidebarFrame)`
- `self.btnCamera.setGeometry(26, 649, 355, 110)` per UI-SPEC section 6.8
- `self.btnCamera.clicked.connect(lambda: self._switch_page(4))`
- `self.nav_buttons.append(self.btnCamera)`

If all correct, no changes needed.

**Guard 2 — Camera page (line 297-303):**
Verify that `if self.camera_results_store is not None:` guard wraps:
- `from .camera_controller import CameraController` (deferred import)
- `self.camera_page = CameraController(self.camera_results_store, parent=self.stackedWidget)`
- `self.stackedWidget.addWidget(self.camera_page)  # Index 4`

If all correct, no changes needed.

**Guard 3 — closeEvent (line 416-417):**
Verify:
- `if hasattr(self, 'camera_page') and self.camera_page and hasattr(self.camera_page, 'stop_timers'):`
- `self.camera_page.stop_timers()`

If all correct, no changes needed.

**Additional audit checks:**
- Verify `_switch_page(4)` is only reachable via `btnCamera.clicked` which only exists inside the camera guard
- Verify `nav_buttons[4]` in `_switch_page()` is safe — button is appended at index 4 only when camera is enabled
- Confirm `camera-icon2.svg` path is correct: `self._icon("camera-icon2.svg")` should resolve to `src/gui/images/camera-icon2.svg`

If all guards are correct (expected based on code read), document the audit result in SUMMARY but make no changes. If any issue is found, fix it.
  </action>
  <verify>
    <automated>cd /media/workspace/smart-saw && python -c "
import sys
with open('src/gui/controllers/main_controller.py') as f:
    content = f.read()
checks = [
    'camera_results_store is not None' in content,
    'camera_page' in content,
    'stop_timers' in content,
    '_switch_page(4)' in content,
]
if all(checks):
    print('MainController audit: all guards present')
    sys.exit(0)
else:
    print('FAIL: missing guard patterns')
    sys.exit(1)
"</automated>
  </verify>
  <acceptance_criteria>
    - main_controller.py contains `if self.camera_results_store is not None:` guard before `self.btnCamera` creation
    - main_controller.py contains `if self.camera_results_store is not None:` guard before `CameraController` import
    - main_controller.py contains `hasattr(self, 'camera_page')` in closeEvent
    - main_controller.py contains `self.camera_page.stop_timers()` in closeEvent
    - `_switch_page(4)` is only connected inside the camera_results_store guard block
    - Verification script exits 0
  </acceptance_criteria>
  <done>MainController camera integration guards verified: sidebar button, camera page, and closeEvent timer stop are all correctly paired inside camera_results_store guard. No functional issues found (or issues fixed if any were discovered).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete camera page</name>
  <files>src/gui/controllers/camera_controller.py, src/gui/controllers/main_controller.py</files>
  <action>
This is a visual verification checkpoint. No code changes — the executor presents the running application to the user for visual confirmation of the complete camera page built across plans 01 and 02.

Run the application with `camera.enabled: true` and navigate to the camera page. Present the full GUI to the user for verification against the UI-SPEC layout.
  </action>
  <what-built>
Complete camera GUI page including:
- Live annotated feed (bounding boxes from DetectionWorker)
- Broken-tooth panel with count, tooth count, timestamp, OK/alert indicator
- Crack panel with count, timestamp, OK/alert indicator
- 4-cell thumbnail strip with 8px spacing
- Wear percentage frame with green-to-red progress bar
- Health score frame with red-to-green progress bar (inverse direction)
- Health status frame with dynamic color from HealthCalculator
- Sidebar camera button (only when camera.enabled=true)
- All sub-label x-offsets at 16px, status font at 22px
  </what-built>
  <how-to-verify>
1. Set `camera.enabled: true` in `config/config.yaml`
2. Run the application: `python run.py`
3. Verify sidebar shows 5 buttons (the 5th is "Kamera" at y=649)
4. Click the "Kamera" sidebar button
5. Verify camera page shows:
   - Live feed panel (top-left, 934x525) — shows "Kamera bekleniyor..." if no camera connected, or live feed with bounding boxes if camera is connected
   - Thumbnail strip (below feed, 4 cells with 8px gaps)
   - Broken-tooth panel (top-right) with count, tooth count, timestamp, "OK" in green 22px text
   - Crack panel (below broken) with count, timestamp, "OK" in green 22px text
   - Wear percentage (bottom-left) with numeric label + green-to-red progress bar below it
   - Health score (bottom-center) with numeric label + red-to-green progress bar below it
   - Health status (bottom-right) with dynamic colored text
6. Verify progress bars have visible gradient (green start for wear, red start for health)
7. Set `camera.enabled: false` in config and restart — verify sidebar has only 4 buttons (no Kamera)
  </how-to-verify>
  <verify>
    <automated>cd /media/workspace/smart-saw && python -m pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>Camera page visually verified by user. All panels, progress bars, sidebar button, and annotated feed render correctly per UI-SPEC.</done>
  <resume-signal>Type "approved" if the camera page looks correct, or describe any visual issues.</resume-signal>
</task>

</tasks>

<verification>
1. `python -c "..."` audit script confirms all guard patterns exist in main_controller.py
2. Visual inspection confirms camera page renders correctly with all panels, progress bars, and sidebar button
3. `python -m pytest tests/ -x -q` — full test suite still passes
</verification>

<success_criteria>
- MainController audit confirms all three guards (button, page, closeEvent) are correct
- Camera page renders visually per UI-SPEC layout
- Sidebar button is conditionally visible based on camera.enabled
- Progress bars display with correct gradient directions
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/24-camera-gui/24-02-SUMMARY.md`
</output>
