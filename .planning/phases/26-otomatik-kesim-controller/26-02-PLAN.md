---
phase: 26-otomatik-kesim-controller
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - src/gui/controllers/otomatik_kesim_controller.py
autonomous: true
requirements:
  - PARAM-01
  - PARAM-03
  - PARAM-04
  - PARAM-05

must_haves:
  truths:
    - "Tapping any param frame opens NumpadDialog with correct initial value"
    - "NumpadDialog acceptance updates the frame value label and internal state"
    - "P or X change recalculates and displays P*X total label"
    - "C acceptance calls MachineControl.write_cutting_speed()"
    - "S acceptance calls MachineControl.write_descent_speed()"
    - "START click with missing P/L/X shows Turkish error message for 3 seconds"
    - "START click with valid params writes to PLC registers then calls start_auto_cutting()"
    - "START button text changes to DEVAM EDIYOR... and disables after successful start"
    - "IPTAL click calls cancel_auto_cutting() and re-enables all params"
    - "Param frame click handlers are guarded by _params_enabled flag"
  artifacts:
    - path: "src/gui/controllers/otomatik_kesim_controller.py"
      provides: "Parameter input handlers, START validation, IPTAL reset"
      contains: "_handle_p_frame_click"
  key_links:
    - from: "otomatik_kesim_controller.py"
      to: "NumpadDialog"
      via: "exec() -> Accepted -> get_value()"
      pattern: "NumpadDialog.*exec"
    - from: "otomatik_kesim_controller.py"
      to: "MachineControl.write_target_adet"
      via: "_handle_start_click"
      pattern: "write_target_adet"
    - from: "otomatik_kesim_controller.py"
      to: "MachineControl.start_auto_cutting"
      via: "_handle_start_click"
      pattern: "start_auto_cutting"
    - from: "otomatik_kesim_controller.py"
      to: "MachineControl.cancel_auto_cutting"
      via: "_handle_iptal_click"
      pattern: "cancel_auto_cutting"
---

<objective>
Wire all parameter input handlers (P, X, L, C, S frame clicks -> NumpadDialog -> value storage -> PLC writes) and control button behavior (START validation + PLC sequence, IPTAL cancel + reset) into OtomatikKesimController.

Purpose: After this plan, the operator can enter all cutting parameters and initiate/cancel auto cutting. The RESET hold-delay, D2056 polling, and ML mode integration are deferred to Plan 03.

Output: Fully functional parameter entry and START/IPTAL control in otomatik_kesim_controller.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-otomatik-kesim-controller/26-CONTEXT.md
@.planning/phases/26-otomatik-kesim-controller/26-RESEARCH.md
@.planning/phases/26-otomatik-kesim-controller/26-UI-SPEC.md
@.planning/phases/26-otomatik-kesim-controller/26-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 scaffold that this plan wires into -->

From otomatik_kesim_controller.py (Plan 01 scaffold):
```python
class OtomatikKesimController(QWidget):
    # State attributes
    self._p_value: str = ""
    self._x_value: str = ""
    self._l_value: str = ""
    self._c_value: str = ""
    self._s_value: str = ""
    self._params_enabled: bool = True
    
    # UI widgets (created in _setup_ui)
    self.frameP, self.labelPValue       # P param frame
    self.frameX, self.labelXValue       # X param frame
    self.frameL, self.labelLValue       # L param frame (allow_decimal=True)
    self.frameC, self.labelCValue       # C param frame
    self.frameS, self.labelSValue       # S param frame
    self.labelTotal                     # "Toplam: {P*X} adet"
    self.btnStart                       # START button
    self.btnIptal                       # IPTAL button
    self.labelValidationError           # Error label (hidden)
    self.labelCounter                   # "0 / 0" counter
    self.progressWidget                 # Progress bar
    self.labelComplete                  # "Tamamlandi!" (hidden)
    
    # Methods
    def _set_params_enabled(self, enabled: bool): ...
    def _update_total_label(self): ...
    def stop_timers(self): ...
```

From NumpadDialog (modified in Plan 01):
```python
NumpadDialog(parent, initial_value=str, allow_decimal=False)
dialog.exec() -> QDialog.Accepted
dialog.get_value() -> str
```

From MachineControl (Phase 25):
```python
machine_control.write_target_adet(p: int, x: int)     # P*X -> D2050
machine_control.write_target_uzunluk(l_mm: float)      # L*10 -> D2064 DWord
machine_control.write_cutting_speed(v: int)             # -> register 2066
machine_control.write_descent_speed(v: float)           # v*100 -> register 2041
machine_control.start_auto_cutting()                    # bit 20.13
machine_control.cancel_auto_cutting()                   # bit 20.4
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire parameter frame click handlers with NumpadDialog</name>
  <files>src/gui/controllers/otomatik_kesim_controller.py</files>
  <read_first>
    - src/gui/controllers/otomatik_kesim_controller.py
    - src/gui/controllers/control_panel_controller.py
    - src/gui/numpad.py
    - .planning/phases/26-otomatik-kesim-controller/26-CONTEXT.md
  </read_first>
  <action>
Add parameter frame click handlers to OtomatikKesimController. Wire them in _setup_ui() after frame creation.

1. Wire mousePressEvent overrides in _setup_ui() after all frames are created:
   - self.frameP.mousePressEvent = self._handle_p_frame_click
   - self.frameX.mousePressEvent = self._handle_x_frame_click
   - self.frameL.mousePressEvent = self._handle_l_frame_click
   - self.frameC.mousePressEvent = self._handle_c_frame_click
   - self.frameS.mousePressEvent = self._handle_s_frame_click

2. Implement _handle_p_frame_click(self, event=None) per D-04, D-06, PARAM-01:
   - First guard: `if not self._params_enabled: return` (Pitfall 2)
   - Accept event if provided
   - Create NumpadDialog(self, initial_value=self._p_value or "")
   - On Accepted: parse as int, clamp max(1, min(9999, value)) per D-06
   - Store in self._p_value as str, update self.labelPValue.setText(str(value))
   - If value <= 0: clear _p_value="", set label to em dash
   - Call self._update_total_label() for D-08 P*X recalculation

3. Implement _handle_x_frame_click(self, event=None) per PARAM-05:
   - Same pattern as P but clamp range max(1, min(999, value)) per D-06
   - Also calls self._update_total_label()

4. Implement _handle_l_frame_click(self, event=None) per D-05, PARAM-02:
   - Same guard pattern
   - Create NumpadDialog(self, initial_value=initial_str, allow_decimal=True) -- the key difference
   - Parse value_str as float via float(value_str.replace(",", "."))
   - Clamp max(1.0, min(99999.0, value)), round to 0.1mm: round(value, 1)
   - Display: f"{value:.1f}" if fractional, else str(int(value))

5. Implement _handle_c_frame_click(self, event=None) per D-07, PARAM-03:
   - Same guard, NumpadDialog(self, initial_value=initial_str) -- no decimal
   - Parse as int, clamp max(0, min(500, value))
   - After storing value: immediately call self.machine_control.write_cutting_speed(value)
   - Do NOT call _update_total_label (C doesn't affect total)

6. Implement _handle_s_frame_click(self, event=None) per D-07, PARAM-04:
   - Same pattern as C
   - After storing: immediately call self.machine_control.write_descent_speed(float(value))
  </action>
  <verify>
    <automated>cd /media/workspace/smart-saw && python -c "
from src.gui.controllers.otomatik_kesim_controller import OtomatikKesimController
methods = ['_handle_p_frame_click', '_handle_x_frame_click', '_handle_l_frame_click', '_handle_c_frame_click', '_handle_s_frame_click', '_update_total_label']
for m in methods:
    assert hasattr(OtomatikKesimController, m), f'missing {m}'
print('PASS: All param handlers exist')
"</automated>
  </verify>
  <acceptance_criteria>
    - otomatik_kesim_controller.py contains `def _handle_p_frame_click(self, event=None)` with guard `if not self._params_enabled: return`
    - otomatik_kesim_controller.py contains `def _handle_x_frame_click(self, event=None)` with range max(1, min(999, value))
    - otomatik_kesim_controller.py contains `def _handle_l_frame_click(self, event=None)` with `allow_decimal=True` in NumpadDialog call
    - otomatik_kesim_controller.py contains `def _handle_c_frame_click(self, event=None)` with `write_cutting_speed(value)` call
    - otomatik_kesim_controller.py contains `def _handle_s_frame_click(self, event=None)` with `write_descent_speed(float(value))` call
    - All 5 frame click handlers check `if not self._params_enabled: return` as first guard
    - P and X handlers both call `self._update_total_label()` after value update
    - L handler uses `allow_decimal=True` and parses value as float with `round(value, 1)`
    - mousePressEvent assignments exist in _setup_ui: `self.frameP.mousePressEvent = self._handle_p_frame_click` etc.
  </acceptance_criteria>
  <done>All 5 param frame click handlers functional: tap opens NumpadDialog, acceptance stores value, updates label, and for C/S writes to PLC immediately. P*X total recalculated on P or X change. All handlers guarded by _params_enabled.</done>
</task>

<task type="auto">
  <name>Task 2: Wire START validation + PLC sequence and IPTAL cancel handler</name>
  <files>src/gui/controllers/otomatik_kesim_controller.py</files>
  <read_first>
    - src/gui/controllers/otomatik_kesim_controller.py
    - .planning/phases/26-otomatik-kesim-controller/26-CONTEXT.md
    - .planning/phases/26-otomatik-kesim-controller/26-RESEARCH.md
  </read_first>
  <action>
Add START and IPTAL button behavior to OtomatikKesimController.

1. Wire button click connections in _setup_ui() after button creation:
   - self.btnStart.clicked.connect(self._handle_start_click)
   - self.btnIptal.clicked.connect(self._handle_iptal_click)

2. Implement _validate_params(self) -> Optional[str] per D-09:
   Returns Turkish error string or None if valid. P, L, X are mandatory (>0). C and S optional:
   - If not self._p_value or int(self._p_value) <= 0: return "P (hedef adet) girilmedi"
   - If not self._l_value or float(self._l_value) <= 0: return "L (uzunluk) girilmedi"
   - If not self._x_value or int(self._x_value) <= 0: return "X (paketteki adet) girilmedi"
   - return None

3. Implement _show_validation_error(self, message: str) per D-09:
   - self.labelValidationError.setText(message)
   - self.labelValidationError.setVisible(True)
   - QTimer.singleShot(3000, lambda: self.labelValidationError.setVisible(False))

4. Implement _handle_start_click(self) per D-09, D-10, Pitfall 6:
   - Call error = self._validate_params()
   - If error: self._show_validation_error(error); return
   - Parse p = int(self._p_value), x = int(self._x_value), l_mm = float(self._l_value)
   - Write to PLC BEFORE starting (Pitfall 6):
     - self.machine_control.write_target_adet(p, x)
     - self.machine_control.write_target_uzunluk(l_mm)
     - If self._c_value: self.machine_control.write_cutting_speed(int(self._c_value))
     - If self._s_value: self.machine_control.write_descent_speed(float(self._s_value))
   - Then: self.machine_control.start_auto_cutting()
   - UI updates per D-10:
     - self.btnStart.setEnabled(False)
     - self.btnStart.setText("DEVAM EDIYOR...")
     - self._set_params_enabled(False)
     - self._cutting_active = True
   - Start polling: if self._polling_timer and not self._polling_timer.isActive(): self._polling_timer.start()

5. Implement _handle_iptal_click(self) per D-12:
   - self.machine_control.cancel_auto_cutting() (guarded by if self.machine_control)
   - self._set_params_enabled(True)
   - self._cutting_active = False
   - self.btnStart.setEnabled(True)
   - self.btnStart.setText("START")
   - self.labelCounter.setText("0 / 0")
   - self._previous_count = None (reset ML detection)
   - Reset progress bar: self.progressWidget._progress = 0.0; self.progressWidget._complete = False; self.progressWidget.update()
   - self.labelComplete.setVisible(False)
   - Stop polling: if self._polling_timer and self._polling_timer.isActive(): self._polling_timer.stop()

6. Implement _get_target(self) -> int helper:
   - try: return int(self._p_value) * int(self._x_value) if both set, else 0
   - except: return 0
  </action>
  <verify>
    <automated>cd /media/workspace/smart-saw && python -c "
from src.gui.controllers.otomatik_kesim_controller import OtomatikKesimController
methods = ['_validate_params', '_show_validation_error', '_handle_start_click', '_handle_iptal_click', '_get_target']
for m in methods:
    assert hasattr(OtomatikKesimController, m), f'missing {m}'
print('PASS: START/IPTAL handlers exist')
" && ruff check src/gui/controllers/otomatik_kesim_controller.py</automated>
  </verify>
  <acceptance_criteria>
    - otomatik_kesim_controller.py contains `def _validate_params(self) -> Optional[str]`
    - _validate_params checks P, L, X mandatory and returns Turkish strings: "P (hedef adet) girilmedi", "L (uzunluk) girilmedi", "X (paketteki adet) girilmedi"
    - _validate_params returns None when all three are valid
    - otomatik_kesim_controller.py contains `def _handle_start_click(self)` with write_target_adet(p, x) BEFORE start_auto_cutting() (Pitfall 6)
    - _handle_start_click calls write_target_uzunluk(l_mm) before start
    - _handle_start_click sets btnStart text to "DEVAM EDIYOR..." and setEnabled(False) per D-10
    - _handle_start_click calls self._set_params_enabled(False) to lock params per D-14
    - otomatik_kesim_controller.py contains `def _handle_iptal_click(self)` calling cancel_auto_cutting()
    - _handle_iptal_click re-enables params via _set_params_enabled(True)
    - _handle_iptal_click resets btnStart text to "START" and setEnabled(True)
    - _handle_iptal_click resets labelCounter text to "0 / 0"
    - otomatik_kesim_controller.py contains `def _show_validation_error(self, message: str)` with QTimer.singleShot(3000, ...)
    - btnStart.clicked.connect(self._handle_start_click) exists in _setup_ui
    - btnIptal.clicked.connect(self._handle_iptal_click) exists in _setup_ui
    - `ruff check src/gui/controllers/otomatik_kesim_controller.py` exits 0
  </acceptance_criteria>
  <done>START button validates P/L/X, shows Turkish error on failure, writes all params to PLC then starts cutting on success. IPTAL cancels cutting, re-enables params, resets all UI state. No regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator input -> PLC registers | User-entered P, L, X, C, S values are clamped to valid ranges before PLC write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03 | Tampering | Parameter values to PLC | mitigate | Value clamping in every frame click handler: P max(1,min(9999)), X max(1,min(999)), L max(1,min(99999)), C/S max(0,min(500)) |
| T-26-04 | Denial of Service | START without PLC values | mitigate | _validate_params() blocks START if P/L/X not set; prevents sending zero/null to PLC registers (Pitfall 6) |
| T-26-05 | Tampering | setEnabled bypass on param frames | mitigate | Python-level guard `if not self._params_enabled: return` in every mousePressEvent handler (Pitfall 2) |
</threat_model>

<verification>
1. `python -c "from src.gui.controllers.otomatik_kesim_controller import OtomatikKesimController"` -- no import errors
2. `ruff check src/gui/controllers/otomatik_kesim_controller.py` -- clean
3. `python -m pytest tests/ -x -q --timeout=10` -- all existing tests pass
4. Grep verification: `grep -n "_validate_params\|_handle_start_click\|_handle_iptal_click\|write_target_adet\|cancel_auto_cutting" src/gui/controllers/otomatik_kesim_controller.py` shows all expected method names
</verification>

<success_criteria>
- All 5 param frame clicks open NumpadDialog, store validated values, update labels
- P*X total label recalculates on P or X change
- C and S immediately write to PLC on acceptance
- START validates, writes registers, then starts cutting
- IPTAL cancels and fully resets UI state
- All frame click handlers guarded by _params_enabled
</success_criteria>

<output>
After completion, create `.planning/phases/26-otomatik-kesim-controller/26-02-SUMMARY.md`
</output>
