# Environment variables
DEBUG=true
LOG_LEVEL=INFO

# PostgreSQL (disabled for now)
# POSTGRES_PASSWORD=your_password

# ThingsBoard (disabled for now)
# TB_ACCESS_TOKEN=your_token

# Test mode (skip Modbus connection)
TEST_MODE=true
